@extends('layouts.app')

@section('content')
<div class="">
    <div class="searchbar mt-0 mb-4">
        <div class="row">
            <div class="col-md-6">
                <form>
                    <div class="input-group">
                        <input
                            id="indexSearch"
                            type="text"
                            name="search"
                            placeholder="{{ __('crud.common.search') }}"
                            value="{{ $search ?? '' }}"
                            class="form-control"
                            autocomplete="off"
                        />
                        <div class="input-group-append">
                            <button type="submit" class="btn btn-primary">
                                <i class="icon ion-md-search"></i>
                            </button>
                        </div>
                    </div>
                </form>
            </div>
            <div class="col-md-6 text-right">
                @can('create', App\Models\OrganizationalUnit::class)
                <a
                    href="{{ route('organizational-units.create') }}"
                    class="btn btn-primary"
                >
                    <i class="icon ion-md-add"></i> @lang('crud.common.create')
                </a>
                @endcan
            </div>
        </div>
    </div>

    <div class="card">
        <div class="card-body">
            <div style="display: flex; justify-content: space-between;">
                <h4 class="card-title">
                    @lang('crud.organizational_units.index_title')
                </h4>
            </div>

            <div class="table-responsive">
                <table class="table table-bordered table-hover">
                    <thead>
                        <tr>
                            <th class="text-left">
                                @lang('crud.organizational_units.inputs.name')
                            </th>
                            <th class="text-right">
                                @lang('crud.organizational_units.inputs.offcie_no')
                            </th>
                            <th class="text-left">
                                @lang('crud.organizational_units.inputs.campuse_id')
                            </th>
                            <th class="text-left">
                                @lang('crud.organizational_units.inputs.building_id')
                            </th>
                            <th class="text-left">
                                @lang('crud.organizational_units.inputs.prioritie_id')
                            </th>
                            <th class="text-center">
                                @lang('crud.common.actions')
                            </th>
                        </tr>
                    </thead>
                    <tbody>
                        @forelse($organizationalUnits as $organizationalUnit)
                        <tr>
                            <td>{{ $organizationalUnit->name ?? '-' }}</td>
                            <td>{{ $organizationalUnit->offcie_no ?? '-' }}</td>
                            <td>
                                {{ optional($organizationalUnit->campuse)->name
                                ?? '-' }}
                            </td>
                            <td>
                                {{ optional($organizationalUnit->building)->name
                                ?? '-' }}
                            </td>
                            <td>
                                {{
                                optional($organizationalUnit->prioritie)->name
                                ?? '-' }}
                            </td>
                            <td class="text-center" style="width: 134px;">
                                <div
                                    role="group"
                                    aria-label="Row Actions"
                                    class="btn-group"
                                >
                                    @can('update', $organizationalUnit)
                                    <a
                                        href="{{ route('organizational-units.edit', $organizationalUnit) }}"
                                    >
                                        <button
                                            type="button"
                                            class="btn btn-light"
                                        >
                                            <i class="icon ion-md-create"></i>
                                        </button>
                                    </a>
                                    @endcan @can('view', $organizationalUnit)
                                    <a
                                        href="{{ route('organizational-units.show', $organizationalUnit) }}"
                                    >
                                        <button
                                            type="button"
                                            class="btn btn-light"
                                        >
                                            <i class="icon ion-md-eye"></i>
                                        </button>
                                    </a>
                                    @endcan @can('delete', $organizationalUnit)
                                    <form
                                        action="{{ route('organizational-units.destroy', $organizationalUnit) }}"
                                        method="POST"
                                        onsubmit="return confirm('{{ __('crud.common.are_you_sure') }}')"
                                    >
                                        @csrf @method('DELETE')
                                        <button
                                            type="submit"
                                            class="btn btn-light text-danger"
                                        >
                                            <i class="icon ion-md-trash"></i>
                                        </button>
                                    </form>
                                    @endcan
                                </div>
                            </td>
                        </tr>
                        @empty
                        <tr>
                            <td colspan="6">
                                @lang('crud.common.no_items_found')
                            </td>
                        </tr>
                        @endforelse
                    </tbody>
                    <tfoot>
                        <tr>
                            <td colspan="6">
                                {!! $organizationalUnits->render() !!}
                            </td>
                        </tr>
                    </tfoot>
                </table>
            </div>
        </div>
    </div>
</div>
@endsection
